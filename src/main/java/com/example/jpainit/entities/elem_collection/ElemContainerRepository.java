package com.example.jpainit.entities.elem_collection;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ElemContainerRepository extends JpaRepository<ElemContainer, Long> {
}
